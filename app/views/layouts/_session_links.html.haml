- if current_user.present?
  = link_to 'logout', destroy_user_session_path, method: :delete, id: 'logout-link', class: 'btn btn-warning'
- elsif controller_name != 'sessions'
  = link_to 'login', new_user_session_path, id: 'login-link', class: 'btn btn-primary'
